let initialStore = {
    friends: [
        { id: 1, name: 'Andre' },
        { id: 2, name: 'Sasha' },
        { id: 3, name: 'Sveta' },
    ]
}

const sidebarReducer = (state = initialStore, action) => {
    
    return state;
}

export default sidebarReducer;